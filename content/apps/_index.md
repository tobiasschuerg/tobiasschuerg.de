---
title: "Apps"
# Container for the per-app policy/terms pages; has no content of its own.
# Those child pages still render — the cascade is deliberately not set here.
build:
  render: never
  list: never
---
